

const ShowCourseName = ({course}) => {
    const {title} = course;
    //console.log(title)
    return (
        <div>
            <h3>{title}</h3>
        </div>
    );
};

export default ShowCourseName;

